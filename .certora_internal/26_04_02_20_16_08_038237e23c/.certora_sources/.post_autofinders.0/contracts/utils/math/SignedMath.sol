// SPDX-License-Identifier: MIT
// OpenZeppelin Contracts (last updated v5.1.0) (utils/math/SignedMath.sol)

pragma solidity ^0.8.20;

import {SafeCast} from "./SafeCast.sol";

/**
 * @dev Standard signed math utilities missing in the Solidity language.
 */
library SignedMath {
    /**
     * @dev Branchless ternary evaluation for `a ? b : c`. Gas costs are constant.
     *
     * IMPORTANT: This function may reduce bytecode size and consume less gas when used standalone.
     * However, the compiler may optimize Solidity ternary operations (i.e. `a ? b : c`) to only compute
     * one branch when needed, making this function more expensive.
     */
    function ternary(bool condition, int256 a, int256 b) internal pure returns (int256) {assembly ("memory-safe") { mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00bd0000, 1037618708669) mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00bd0001, 3) mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00bd0005, 73) mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00bd6002, b) }
        unchecked {
            // branchless ternary works because:
            // b ^ (a ^ b) == a
            // b ^ 0 == b
            return b ^ ((a ^ b) * int256(SafeCast.toUint(condition)));
        }
    }

    /**
     * @dev Returns the largest of two signed numbers.
     */
    function max(int256 a, int256 b) internal pure returns (int256) {assembly ("memory-safe") { mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00be0000, 1037618708670) mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00be0001, 2) mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00be0005, 9) mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00be6001, b) }
        return ternary(a > b, a, b);
    }

    /**
     * @dev Returns the smallest of two signed numbers.
     */
    function min(int256 a, int256 b) internal pure returns (int256) {assembly ("memory-safe") { mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00c00000, 1037618708672) mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00c00001, 2) mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00c00005, 9) mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00c06001, b) }
        return ternary(a < b, a, b);
    }

    /**
     * @dev Returns the average of two signed numbers without overflow.
     * The result is rounded towards zero.
     */
    function average(int256 a, int256 b) internal pure returns (int256) {assembly ("memory-safe") { mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00c10000, 1037618708673) mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00c10001, 2) mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00c10005, 9) mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00c16001, b) }
        // Formula from the book "Hacker's Delight"
        int256 x = (a & b) + ((a ^ b) >> 1);assembly ("memory-safe"){mstore(0xffffff6e4604afefe123321beef1b02fffffffffffffffffffffffff0000007f,x)}
        return x + (int256(uint256(x) >> 255) & (a ^ b));
    }

    /**
     * @dev Returns the absolute unsigned value of a signed value.
     */
    function abs(int256 n) internal pure returns (uint256) {assembly ("memory-safe") { mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00bf0000, 1037618708671) mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00bf0001, 1) mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00bf0005, 1) mstore(0xffffff6e4604afefe123321beef1b01fffffffffffffffffffffffff00bf6000, n) }
        unchecked {
            // Formula from the "Bit Twiddling Hacks" by Sean Eron Anderson.
            // Since `n` is a signed integer, the generated bytecode will use the SAR opcode to perform the right shift,
            // taking advantage of the most significant (or "sign" bit) in two's complement representation.
            // This opcode adds new most significant bits set to the value of the previous most significant bit. As a result,
            // the mask will either be `bytes32(0)` (if n is positive) or `~bytes32(0)` (if n is negative).
            int256 mask = n >> 255;

            // A `bytes32(0)` mask leaves the input unchanged, while a `~bytes32(0)` mask complements it.
            return uint256((n + mask) ^ mask);
        }
    }
}
